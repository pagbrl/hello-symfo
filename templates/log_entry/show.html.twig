{% extends 'base.html.twig' %}

{% block title %}LogEntry{% endblock %}

{% block body %}
    <h1>LogEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ log_entry.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ log_entry.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ log_entry.content }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ log_entry.timestamp ? log_entry.timestamp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_log_entry_index') }}">back to list</a>

    <a href="{{ path('app_log_entry_edit', {'id': log_entry.id}) }}">edit</a>

    {{ include('log_entry/_delete_form.html.twig') }}
{% endblock %}
